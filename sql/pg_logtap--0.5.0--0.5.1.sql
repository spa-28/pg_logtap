/* 0.5.0 → 0.5.1: no SQL-visible change (regex compilation and release
   packaging only). The hop exists because every version needs one for
   ALTER EXTENSION UPDATE. Binary replace + restart, as always. */
