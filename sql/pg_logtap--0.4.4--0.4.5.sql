/* 0.4.4 → 0.4.5: no SQL-visible change — the seq-seed clamp and the
   delivery-contract documentation live in the binary and the docs. The hop
   exists because every version needs one (the ALTER EXTENSION UPDATE chain
   must stay continuous); installs on 0.4.4 pass through here to 0.5.0.
   Binary replace + restart, as always. */
